{
  "name": "nightmare",
  "version": "1.0.0",
  "description": "Optional extension for possession, enables arbitrary code execution",
  "scripts": {
    "lint": "eslint .",
    "build": "vite build",
    "clean": "rm -rf dist",
    "package": "web-ext build -s dist -o",
    "bundle": "pnpm clean && pnpm build && pnpm package"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/firefox-webext-browser": "^120.0.4",
    "vite": "^7.1.3",
    "vite-plugin-web-extension": "^4.4.5"
  }
}
